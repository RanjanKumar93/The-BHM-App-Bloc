import 'package:flutter/material.dart';

class RebateScreen extends StatelessWidget {
  const RebateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}